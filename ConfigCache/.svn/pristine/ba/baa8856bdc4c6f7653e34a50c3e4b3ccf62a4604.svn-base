package org.config.cache.decode.text;

import org.config.cache.StringArray;
import org.config.cache.core.AbstractDecoder;
import org.config.cache.data.BuildingConfig;

/**
 * 建筑配置表行解析器
 * @author chenjie
 * 2012-12-12
 */
public class BuildingDecoder extends AbstractDecoder<BuildingConfig> {

	@Override
	public BuildingConfig decode(StringArray values) {
		
		BuildingConfig config = new BuildingConfig();
		config.fromStringArray(values);
		
		return config;
	}

}
