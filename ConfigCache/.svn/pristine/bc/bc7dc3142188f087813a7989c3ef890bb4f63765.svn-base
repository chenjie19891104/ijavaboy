package org.config.cache.decode.text;

import org.config.cache.StringArray;
import org.config.cache.core.AbstractDecoder;
import org.config.cache.data.BuildingPositionConfig;

/**
 * 建筑位配置表行解析器
 * @author chenjie
 * 2012-12-12
 */
public class BuildingPositionDecoder extends AbstractDecoder<BuildingPositionConfig> {

	@Override
	public BuildingPositionConfig decode(StringArray values) {
		BuildingPositionConfig config = new BuildingPositionConfig();
		config.fromStringArray(values);
		return config;
	}

}
